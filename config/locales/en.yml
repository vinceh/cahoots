# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  activerecord:
    attributes:
      user:
        tos: ""
        email: "Email"
    errors:
      models:
        user:
          attributes:
            tos:
              accepted: "You must accept our 'terms of service' to create an account"
            email:
              taken: "is already associated with an account"
